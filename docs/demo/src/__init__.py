"""waitbus_demo: stub package the test suite imports.

The waitbus-demo repository ships a token Python package so the
`pytest` invocation has something concrete to collect. The package
body is intentionally empty; the value is in the tests, not in any
shipped code.
"""

__version__ = "0.0.0"
